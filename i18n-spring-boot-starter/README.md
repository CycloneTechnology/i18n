# 🌍 Spring I18n Starter

A Spring Boot 3.1 autoconfiguration starter for internationalization using YAML message files. 
Supports deeply nested keys, numeric identifiers, runtime interpolation, and automatic locale 
fallback — all wired seamlessly into Spring’s `MessageSource`.

For more detail see the README in the library i18n project.
